  <%= link_to tractor do  %>
    <div class="info-container rounded">
      <div class="d-flex flex-column justify-content-between align-items-center">
        <%= cl_image_tag tractor.photo.key, width: 60, height: 60, crop: :fill, class: 'rounded-circle' %>
        <h6><%= tractor.title %></h6>
        <p class="price-map"> <%= tractor.price_per_day %>€</p>
      </div>
    </div>
  <% end %>
